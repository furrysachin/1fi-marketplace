{
  "name": "1fi-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "1Fi Marketplace — browse smartphones and pay via mutual-fund-backed EMI plans",
  "scripts": {
    "setup": "npm install && npm --prefix server install && npm --prefix client install",
    "seed": "npm --prefix server run seed",
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}