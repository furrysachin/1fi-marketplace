# Port the API listens on
PORT=5000

# development | production (production also serves the built client)
NODE_ENV=development

# Path to the SQLite database file (relative to the server/ folder)
DB_PATH=./data/marketplace.db